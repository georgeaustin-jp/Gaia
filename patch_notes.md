# Patch notes

*v0.1.3-alpha*

 * Re-formatted some exceptions
 * Removed redundant dependencies
 * Added poetry dependency handling
 * Removed config file 
 * Removed miscellaneous unnecessary files
 * Added labels to distinguish weapons and equipables
 * Fixed effect messages not being added
 * Fixed the ability to add invalid worlds and characters
 * Fixed bug where multiple bosses would try to spawn
 * Modified state-resetting methods
 * Fixed off-by-one error in structure item generation
 * Increased probability of encountering structures
 * Slightly increased probability of encountering combat
 * Slightly increased boss encounter probability
 * Reduced health potion heal amount (30HP -> 25HP)
 * Fixed bug where deleted items would still try to swap inventories if left selected when deleted
 * Fixed bug where weapon data loaded to the interface would not match that of the data loaded to the weapon itself
 